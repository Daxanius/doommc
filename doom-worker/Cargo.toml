[package]
name = "doom-worker"
version = "0.1.0"
edition = "2024"

[dependencies]
doomgeneric = { path = "../doomgeneric-rs" }
doom-protocol = { path = "../doom-protocol" }
postcard = { version = "1.0", features = ["alloc"] }
mpsc = "0.2.6"
