[package]
name = "doom-server"
version = "0.1.0"
edition = "2021"

[dependencies]
doom-protocol = { path = "../doom-protocol" }
valence = { version = "0.2.0-alpha.1+mc.1.20.1", features = [
    "inventory",
    "network",
] }
rand = "0.9.2"
mpsc = "0.2.6"
postcard = "1.1.3"

# [[bin]]
# name = "doom-worker"
# path = "../doom-worker/"
